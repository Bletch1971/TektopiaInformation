package bletch.tektopiainformation.enums;

/*
 * The order of these enums is used to determine the z order when displayed on the maps.
 */
public enum GuiMapMarkerType {
	UNKNOWN,
	STRUCTURE,
	RESIDENT,
	PLAYER,
}
